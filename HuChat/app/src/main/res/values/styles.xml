<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/orangeLogoColor</item>
        <item name="android:statusBarColor">@color/grayColor</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorAccent">@color/greenMenuBarColor</item>
    </style>

</resources>
